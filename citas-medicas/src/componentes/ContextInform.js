import { createContext } from 'react'

const ContextInform = createContext ({
    nombre: null
   /*  email: null,
    telefono: null,
    dia: null,
    hora: null,
    sintomas: null */
});

export default ContextInform;